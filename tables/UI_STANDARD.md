# UI_STANDARD.md — ПЛЮС Таблиці

Статус: локальний довідник редактора.

Глобальні правила дивись у `../UI_INTEGRATION_GUIDE.md` та `../OFFICE_UI_STANDARD.md`. Архітектурну межу між shared root-шаром і локальним шаром редактора дивись у `../ARCHITECTURE.md`.

## Специфіка

- Тип: табличний редактор.
- Основні сценарії: редагування клітинок, імпорт/експорт workbook, CSV і XLSX, базове форматування, формули, сортування та діаграми.
- Меню: Файл, Редагування, Вставка, Формат, Дані, Перегляд, Допомога.
- Стандартні команди мають іти через `OfficeUI.registerCommands` / `OfficeUI.runCommand`.
- Відкриття workbook має іти через `OfficeUI.openFilePicker`.

## Навчальна ціль

ПЛЮС Таблиці мають розвиватися як шкільний аналог Excel/Google Sheets:

- клітинки, діапазони, копіювання, вставлення, очищення, undo/redo;
- формули для арифметики, статистики, відсотків, округлення і простих умов (`SUM`, `AVG`, `MIN`, `MAX`, `COUNT`, `COUNTA`, `MEDIAN`, `ROUND`, `MOD`, `IF`, `AND`, `OR`, `NOT`);
- фінансові функції `PV(rate, nper, pmt, [fv], [type])`, `FV(rate, nper, pmt, [pv], [type])`, `PMT(rate, nper, pv, [fv], [type])` — сигнатури й формули Excel:
  - `rate` — ставка за період (річна / 12 для щомісячних платежів);
  - сплачене — від'ємне, отримане — додатне;
  - `type` 0 або пропущено — наприкінці періоду, будь-яке інше число — на початку;
  - нульова ставка рахується окремою формулою;
  - неправильна кількість аргументів або текст — `#VALUE!`;
  - нуль періодів або ставка ≤ −1 у `PMT`, `0^0` (ставка −1 і нуль періодів у `PV`/`FV`) і переповнення `PV`/`FV` — `#NUM!`;
  - `PV` зі ставкою −1 — `#DIV/0!`;
  - `PMT` не переповнюється на великій кількості періодів (як Excel: `PMT(0,1;100000;1000)` = −100);
  - пропуск середнього аргументу двома комами (`PMT(r;n;pv;;1)`) не підтримується;
  - у XLSX пишуться з типізованим кешем, бо результат збігається з Excel;
- іменовані діапазони книги («Дані → Іменовані діапазони…», `tables/js/named-ranges.js`):
  - перша версія — лише імена рівня книги на прямокутний діапазон одного аркуша; іменовані формули, об'єднання діапазонів та імена рівня аркуша не підтримуються;
  - ім'я починається з літери (латиниця або кирилиця) чи `_`, далі літери, цифри та `_`, до 60 символів; до 100 імен у книзі;
  - унікальність без урахування регістру; не можна імена, схожі на адресу (`A1`, `Tax2024`), `TRUE`, `FALSE`, `R`, `C` і назви функцій;
  - у формулі ім'я — окремий вузол AST (`=SUM(Витрати)`, `=Ставка*2`), а не текстова підстановка; невідоме ім'я — `#NAME?`, багатоклітинне ім'я як одне значення — `#VALUE!`;
  - перейменування переписує токени-імена в усіх формулах; вставка/видалення рядків і колонок зсуває діапазон; видалення всіх клітинок імені або його аркуша лишає ім'я зламаним (`#REF!`); перейменування аркуша оновлює ім'я;
  - імена входять в undo/redo, чернетку та `.arttab` версії 3; файли версій 1–2 відкриваються без імен;
  - XLSX: імпорт `definedName` рівня книги на абсолютний діапазон (`Дані!$B$2:$B$4`), решта (рівня аркуша, несумісні з синтаксисом ПЛЮС, формули й кілька діапазонів, службові `_xlnm.`) названа в попередженні; експорт пише `<definedNames>`, а зламані імена перелічує `describeExportLosses`;
- рядки й колонки з коректним зсувом даних, стилів і формульних посилань;
- форматування чисел, тексту, меж, заливок і вирівнювання;
- сортування, базові підсумки, виділені діапазони й навчальні приклади;
- діаграми на аркуші («Вставка → Побудувати діаграму», `tables/js/chart-model.js`, `tables/js/charts.js`):
  - стовпчаста, лінійна й кругова діаграми; до 12 рядів, до 500 точок і до 20 діаграм на аркуші;
  - діапазони зберігаються явно: категорії, клітинка назви ряду й значення — одновимірні діапазони того самого аркуша. З виділення перша колонка стає категоріями, якщо колонок дві або в ній є текст, а текстовий перший рядок — назвами рядів;
  - діаграма плаває над сіткою, прив'язана до клітинки-якоря; позиція рахується від реальних меж намальованих колонок і кроку рядків, тож діаграма на краю віртуалізованої сітки лишається на місці;
  - рамка має тип, назву, видалення, перетягування й зміну розміру; з клавіатури: стрілки — перемістити на клітинку, Shift+стрілки — розмір, Delete — видалити;
  - вставка й видалення рядків і колонок зсувають діапазони та якір; видалений ряд зникає, а діаграма без рядів показує повідомлення; видалення аркуша прибирає його діаграми; усе це входить в undo/redo і чернетку;
  - `.arttab` версії 4 зберігає діаграми аркушів і перевіряє їх під час відкриття; файли версій 1–3 відкриваються без діаграм;
  - обмеження: діаграма не бачить фільтр рядків (позиція рахується без схованих рядків), горизонтальних, накопичувальних, точкових та інших типів немає;
- імпорт/експорт CSV і локального workbook без втрати базових форматів;
- локальний XLSX v1: до 50 аркушів і 500×200 клітинок на аркуш; числа, текст, ISO-дати, базові формули, ширини колонок та базові стилі. Діаграми (`tables/js/xlsx-charts.js`): експорт пише малюнок аркуша (`oneCellAnchor`), chart XML з посиланнями й кешами значень та зв'язки; імпорт переносить стовпчасту (clustered column), лінійну й кругову діаграми з рядами на тому ж аркуші, а решту (інші типи, дані з іншого аркуша чи кількох діапазонів, зображення й фігури) називає в попередженні. Merge, зображення, pivot, макроси й складні формати не переносяться та мають бути явно названі користувачеві. Формули експортуються з типізованим кешем (`evaluateFormulaForExport`: число, логічне, текст або код помилки Excel) лише там, де ПЛЮС і Excel рахують однаково; для решти `<v>` не пишеться, а книга має `fullCalcOnLoad="1"`. Корпус і результати відкриття в Microsoft Office — `tests/fixtures/compatibility/`. Перед експортом XLSX `describeExportLosses` перелічує лише актуальні втрати поточної книги (умовне форматування, грошовий формат ₴) і дає «Скасувати», «Зберегти .arttab» та «Експортувати XLSX»; книга без втрат експортується без діалогу, а експорт зі втратами не знімає позначку незбережених змін.

## Локальна структура

- `tables/index.html` — HTML shell редактора.
- `tables/style.css` — локальні стилі редактора.
- `tables/js/runtime.js` — стабільний runtime entrypoint без бізнес-логіки.
- `tables/js/app.js` — boot і shell-adapter (`TablesApp.boot`, із сумісним alias `initTablesApp`).
- `tables/js/addressing.js` — адресація колонок, A1-посилання, діапазони і зсуви формул.
- `tables/js/model.js` — розміри сітки і спільний стан workbook.
- `tables/js/storage.js` — IndexedDB persistence через спільний `OfficeStorage`, міграція legacy LocalStorage та сумісні load/persist/flush/clear wrappers.
- `tables/js/formula-parser.js` — безпечний арифметичний parser і розбір аргументів функцій.
- `tables/js/formula-references.js` — читання значень клітинок, виразів і умов.
- `tables/js/formula-functions.js` — реалізації spreadsheet-функцій (`SUM`, `IF`, `ROUND`, `MOD` тощо).
- `tables/js/formula-engine.js` — координатор `evaluateFormula`.
- `tables/js/named-ranges.js` — іменовані діапазони книги: правила імен, розв'язання в посилання для рушія, зсуви при зміні структури, нормалізація для файлів і діалог «Іменовані діапазони».
- `tables/js/core.js` — сумісний фасад і точка ініціалізації core-шарів.
- `tables/js/state.js` — локальний runtime state і app-core шар.
- `tables/js/column-sizing.js` — зміна ширини колонок і застосування colgroup-розмірів.
- `tables/js/formula-bar.js` — вставка посилань клітинок у формульну панель і майбутній майстер формул.
- `tables/js/grid.js` — grid/rendering логіка.
- `tables/js/grid-viewport.js` — видиме вікно великої сітки, overscan і model-coordinate навігація без зміни workbook-моделі.
- `tables/js/xlsx-file.js` — локальний OOXML/ZIP adapter; не використовує CDN і не додає third-party bundle.
- `tables/js/xlsx-charts.js` — діаграми XLSX: DrawingML chart, малюнок аркуша й зв'язки для експорту, розбір діаграм і перелік втрат для імпорту.
- `tables/js/clipboard.js` — копіювання, TSV-серіалізація і вставлення табличних діапазонів.
- `tables/js/selection-actions.js` — очищення виділення і вставка швидких функцій для діапазонів.
- `tables/js/formatting.js` — стилі клітинок, перемикання форматів і автоширина колонок.
- `tables/js/structure.js` — операції з рядками, колонками, структурними змінами і кнопками швидкої вставки.
- `tables/js/workbook.js` — CSV, приклади та базові workbook-сценарії.
- `tables/js/chart-model.js` — модель діаграм аркуша без DOM: нормалізація, діапазони з виділення, зсув при зміні структури, дані для малювання.
- `tables/js/charts.js` — діаграми на аркуші: створення з виділення, плаваючі рамки Chart.js над сіткою, тип, назва, переміщення, розмір і видалення.
- `tables/js/ui.js` — DOM/UI-шар редактора: меню, модалки, палітри, dispatch UI actions.
- `tables/js/sorting.js` — сортування виділених діапазонів.
- `tables/js/workbook-file.js` — експорт, імпорт і відкриття локального `.arttab` workbook.
- `tables/js/view-options.js` — zoom, theme/view-параметри редактора.
- `tables/js/cell-format-ui.js` — стан toolbar, статус виділення і форматоване відображення значень.
- `tables/js/calculation.js` — перерахунок клітинок, відображення результатів формул і помилок.

## Локальні пріоритети

- Не ламати ввід у клітинках глобальними hotkeys.
- Тримати header/context menus синхронізованими з `office:overlayclose`.
- Уникати дублювання formatting controls між toolbar і контекстом.
- Не збільшувати кількість JS-файлів без чіткої доменної межі або тестової користі.
- Перед новими великими функціями стабілізувати базові сценарії: boot, формули, виділення, форматування, import/export, діаграми.

## Надійність великої сітки

- Зміни клітинки планують локальний запис під час `input`; blur, `visibilitychange` і `pagehide` виконують flush.
- Аркуші понад 10 000 модельних клітинок рендеряться вікном з overscan 5 рядків і 2 колонки.
- Формули обчислюються з workbook-моделі й не залежать від присутності клітинки в DOM.
- CSV-експорт префіксує сирий текст, що починається з `=`, `+`, `-`, `@`, табуляції або carriage return.

## Поточна оцінка технічного боргу

Розділення `core.js` і `formula-engine.js` зменшило ризик моноліту і зробило формульну логіку читабельнішою. Основний залишковий ризик не в розмірі окремого файлу, а в classic-script глобальних залежностях між модулями. Тому наступні технічні кроки мають підсилювати контракти, тести й поведінку, а не просто додавати нові файли.
